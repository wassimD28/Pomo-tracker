export enum Entity {
  CATEGORY,
  TASK,
  TASK_COMPONENT,
  SETTING,
  POMODORO_SESSION,
}

export enum ContentType {
  text = "text",
  image = "image",
  link = "link",
}
export enum AvailableContentType {
  text = "text",
  image = "image",
}
